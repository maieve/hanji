export type ToolKind='pen'|'fountainPen'|'monoline'|'pencil'|'crayon'|'watercolor'|'marker'|'eraser'|'lasso'|'shape';
export type ShapeKind='line'|'arrow'|'ellipse'|'rectangle'|'triangle'|'polygon';
export type PageRotation=0|90|180|270;
export type PageTemplate='plain'|'line'|'grid'|'dot'|'cornell'|'planner'|'flashcard'|'dark';
export type TemplateSpacing='narrow'|'medium'|'wide';
export type PagePaintDirection='vertical'|'horizontal'|'diagonalDown'|'diagonalUp'|'radial';
export type CanvasExtent={columns:number;rows:number};
export type DrawingViewport={x:number;y:number;width:number;height:number;canvasWidth:number;canvasHeight:number};
export type ToolSpec={kind:ToolKind;color:string;width:number;opacity?:number;eraserMode?:'vector'|'bitmap'|'fixedWidthBitmap';eraserAutoReturn?:boolean;rulerActive?:boolean;lassoMode?:'freeform'|'rectangle';lassoInk?:boolean;lassoText?:boolean;lassoImages?:boolean;shapeKind?:ShapeKind;shapeLineStyle?:'solid'|'dashed';shapeFillStyle?:'none'|'translucent'|'solid';shapeHoldRequired?:boolean;scratchEnabled?:boolean;circleToLasso?:boolean;markerStraightLine?:boolean};
export type NativeStrokePoint={x:number;y:number;t:number;force:number;azimuth:number;altitude:number;width:number;height:number;opacity:number};
export type NativeStroke={id:string;ink:string;color:string;createdAt:number;points:NativeStrokePoint[]};
export type OcrWord={text:string;confidence:number;x:number;y:number;width:number;height:number;coordinateSpace?:'canvas'};
export type TextElement={id:string;kind:'text';text:string;x:number;y:number;width:number;height:number;fontSize:number;color:string;source?:{notebookId:string;pageId:string;pageIndex:number;pdfName?:string}};
export type ImageElement={id:string;kind:'image';uri:string;x:number;y:number;width:number;height:number;fit?:'contain'|'cover';rotation?:0|90|180|270;cropZoom?:number;cropX?:number;cropY?:number};
export type Sticker={id:string;name:string;uri:string;width:number;height:number;fit?:'contain'|'cover';rotation?:0|90|180|270;cropZoom?:number;cropX?:number;cropY?:number;createdAt:string};
export type PageElement=TextElement|ImageElement;
export type Page={id:string;drawingData:string;drawingViewport?:DrawingViewport;template:PageTemplate;templateSpacing?:TemplateSpacing;backgroundColor?:string;backgroundColor2?:string;backgroundGradientDirection?:PagePaintDirection;backgroundOpacity?:number;rotation?:PageRotation;canvasExtent?:CanvasExtent;customTemplateUri?:string;updatedAt:string;bookmarked?:boolean;elements?:PageElement[];pdfUri?:string;pdfName?:string;pdfPageIndex?:number;ocrText?:string;ocrWords?:OcrWord[];ocrLineCount?:number;ocrAverageConfidence?:number;ocrRecognizedAt?:string};
export type StrokeSync={pageId:string;strokeId?:string;createdAt:number;seekSec:number};
export type StrokeEvent={id?:string;createdAt:number;maxX?:number;maxY?:number};
export type TranscriptSegment={text:string;start:number;duration:number;confidence:number};
export type AudioSession={uri:string;createdAt:string;startedAt:number;durationMs:number;strokes:StrokeSync[];transcript?:string;transcriptSegments?:TranscriptSegment[];transcribedAt?:string};
export type Flashcard={id:string;question:string;answer:string;questionImageUri?:string;answerImageUri?:string;createdAt:string;updatedAt:string;dueAt:string;intervalDays:number;easeFactor:number;repetitions:number;lapses:number;lastReviewedAt?:string};
export type Notebook={id:string;title:string;folder:string;tags:string[];favorite:boolean;locked?:boolean;coverColor?:string;coverUri?:string;createdAt:string;updatedAt:string;lastOpenedAt?:string;pageOrderUpdatedAt?:string;viewMode?:'page'|'continuous';pages:Page[];deletedPages?:Record<string,string>;audioSessions?:AudioSession[];flashcards?:Flashcard[];conflictOf?:string;conflictSignature?:string};
